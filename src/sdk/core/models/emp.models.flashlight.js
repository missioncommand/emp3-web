/*globals emp */
emp.models = emp.models || {};
emp.models.flashlight = (function () {
  var publicInterface;

  publicInterface = {
    construct: function (/*def*/) {
      /*var modelInstance = {
       id: emp.helpers.uuid(),
       feature: {}
       }*/
    },
    validate: function (/*model*/) {

    }
  };

  return publicInterface;
}());
